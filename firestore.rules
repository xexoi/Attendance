rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public access for all collections as requested (no authentication)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
